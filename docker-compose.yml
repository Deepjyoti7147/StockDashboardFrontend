version: "3.8"
services:
  stock-dashboard:
    build: .
    container_name: stock-dashboard
    restart: unless-stopped
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    mem_limit: 64m
